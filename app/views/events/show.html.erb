<div class="jumbotron">
  <h2><%=@event.title%></h2>
  <p><%=@event.description%></p>
  <p>Start date: <%=@event.start_date.strftime("%A, %B %d, %Y at %H:%M") if @event.start_date %></p>
  <p>End date: <%=@event.end_date.strftime("%A, %B %d, %Y at %H:%M") if @event.end_date%> </p>
  <p>Recurs:
    <% if @event.recurring.blank? %>
      Never
    <% else %>
      <%= "#{@event.recurring[:interval]}" %>
    <% end %>
  </p>
</div>
<% if current_user %>
<div class="col-md-6">
  <%= link_to('Edit', edit_event_path(@event), class: 'btn btn-success btn-block', id: 'edit-button') %>
</div>
<% end %>
<%= render partial: 'shared/map_scripts', locals: {markers: @markers} %>
