<%= link_to org.name, org, :style => "font-weight: 100" %></strong></br>
<ul>
<% VolunteerOp.where(organisation_id: org.id).each do |op| %>
  <li>
    <%= link_to op.title, op, :style => "font-weight: 900" %></br>
    <%= smart_truncate(op.description,32) %></br>
  </li>
  <% end %>
  </ul>
</div>
