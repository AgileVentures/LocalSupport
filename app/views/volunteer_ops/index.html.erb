<br>
<div id="orgs_scroll" style="height:340px;width:100%;border:1px solid #ccc;overflow:scroll;">
  <div class="table table-bordered">
    <table>
      <% @volunteer_ops.each do |op| %>
          <tr>
            <td>
              <strong><%= link_to op.title, op %></strong><br/>

              <div id="organizations">
                <small><%= link_to op.organization.name, op.organization %></small>
                <br/>
              </div>
              <%= smart_truncate(op.description) %>
              <br/>
            </td>
          </tr>
      <% end %>
    </table>
  </div>
</div>

<% render partial: 'shared/map_scripts', locals: {json: @json} %>
