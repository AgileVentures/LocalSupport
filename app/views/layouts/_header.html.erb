<header>

  <div class="navbar  navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <%= link_to(image_tag("HCN-long.png", :class => "img-responsive" ), root_path) %>

      </div>
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a href="/" class="brand" title="Some SEO stuff....">LocalSupport</a>

        <div class="nav-collapse collapse">

          <ul class="nav nav-pills pull-right">
            <% if !(user_signed_in?)%>
                <li class="dropdown " id="menuLogin">
                  <a class="dropdown-toggle" href="#" data-toggle="dropdown" id="navLogin">Login</a>
                  <div class="dropdown-menu" style="padding:17px;">
                    <label>Login:</label>
                    <%= form_for(resource, :html => { :id => "loginForm" }, :as => resource_name, :url => session_path(resource_name)) do |f| %>
                        <div> <%= f.email_field :email, :autofocus => true, :placeholder => "you@example.com" %></div>

                        <div><%= f.password_field :password, :placeholder => "password" %></div>

                        <% if devise_mapping.rememberable? -%>
                            <div><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
                        <% end -%>

                        <div><%= f.submit "Sign in", :id => 'signin', :class => "btn btn-primary" %></div>
                    <% end %>
                    <form><a href="#" title="Some SEO stuff..." data-toggle="collapse" data-target="#registerForm">Sign-up..</a></form>

                    <%= form_for(resource, :html => { :id => "signupForm" }, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
                        <%= devise_error_messages! %>
                          <%= f.email_field :email, :autofocus => true, :id => 'signup_email', :name => 'user[email]', :placeholder => "you@example.com" %>
                          <%= f.password_field :password, :id => 'signup_password', :name => 'user[password]', :placeholder => "password" %>
                          <%= f.password_field :password_confirmation, :id => 'signup_password_confirmation', :placeholder => 'confirm password' %>

                        <div><%= f.submit "Sign up" %></div>
                    <% end %>
                  </div>
                </li>
            <% end %>
            <% if (user_signed_in?)%>
                <li class="dropdown">
                  <a href="#" id="<%= current_user.email %>" class="dropdown-toggle" data-toggle="dropdown" title="Some SEO stuff....">
                    <!--i class="icon-user icon-xxlarge"> </i--> <%= current_user.email %>
                    <b class="caret"></b>
                  </a>

                  <ul class="dropdown-menu">
                    <li><a href="#">Kan be hidden if not logged in</a></li>
                    <li><a href="#">My organization</a></li>
                    <li class="divider"> </li>
                    <li><a href="#">Edit profile</a></li>
                    <li class="divider"> </li>
                    <li><%=link_to "Not you?", destroy_user_session_path, :method => :delete%></li>
            <% end %>
            </ul>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>







  <!--header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
       <div class="row">
        <div class="span6">
          <%= link_to(image_tag("HCN2.png", :style => "width:200px;height:72px;" ), root_path) %>
          <%= image_tag("beta.jpg", :style => "width:75px;") %>
        </div>
        <% if (user_signed_in?)%>
       <div class="span6">
         Signed in as <%= current_user.email %>.  <%=link_to "Not you?", destroy_user_session_path, :method => :delete%>
       </div>
       <% end %>
      <div class="span6">
      <nav>
        <ul class="nav pull-right" style="padding-top:17px;">
          <li><%= link_to "Home", root_path %></li>
          <li><% if (user_signed_in?)%>
          <%=link_to "Sign Out", destroy_user_session_path, :method => :delete%>
          <% end %></li>
          <li><% if !(user_signed_in?)%>
          <%= link_to "New Org?", new_user_registration_path %>
          <% end %></li>
          <li><% if !(user_signed_in?)%>
          <%= link_to "Org Login", user_session_path %>
          <% end %></li>
          <li><%= link_to 'Disclaimer', page_path('disclaimer') %></li>
        </ul>
      </nav>
      </div>

  </div>
  </div>
  </div-->



</header>