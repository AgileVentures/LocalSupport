<% if @organization.users.empty? %>
    <div>This organization has no admins yet</div>
<% else %>
  <div>
  Organization administrator emails:
  <ol>
  <% @organization.users.each do |admin| %>
  <li><%= admin.email %></li>
  <% end %>
  </ol>
  </div>
<% end %>