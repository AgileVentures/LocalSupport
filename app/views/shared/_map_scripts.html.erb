<% content_for :map do %>
    <%= gmaps("map_options" => {"auto_adjust" => false,
                                "auto_zoom" => true,
                                "center_latitude" => 51.5978,
                                "center_longitude" => -0.3370,
                                "zoom" => 12},
              "markers" => {"data" => @json},
              "options" => {"custom_infowindow_class" => "ls_infobox"}) %>
<% end %>

<% content_for :scripts do %>
    <%= javascript_include_tag 'infobox' %>
<% end %>